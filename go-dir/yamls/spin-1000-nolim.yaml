apiVersion: v1
kind: Pod
metadata:
  name: testtime
  labels:
    app: command-demo
spec:
  containers:
  - name: command-container
    image: sleepymug/golang-timing-test
    command: ["./go-test", "spin", "1000"]
    resources:
      limits:
        
        memory: 4Gi
  restartPolicy: Never