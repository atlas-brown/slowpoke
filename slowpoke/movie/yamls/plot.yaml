kind: Service
apiVersion: v1
metadata:
  name: "plot"
  labels:
    app: "plot"
spec:
  selector:
    app: "plot"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      name: http
    - protocol: TCP
      port: 5550
      targetPort: 5550
      name: zmq1
    - protocol: TCP
      port: 5551
      targetPort: 5551
      name: zmq2
  type: LoadBalancer

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "plot"
  labels:
    app: "plot"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "plot"
  template:
    metadata:
      labels:
        app: "plot"
    spec:
      containers:
        - name: "plot"
          image: yizhengx/mucache:movie-pokerpp
          env:
            - name: APP_PORT
              value: "3000"
            - name: APP_NAME
              value: "plot"
            - name: SLOWPOKE_SERV_NAME
              value: "plot"
            - name: APP_NAMESPACE
              value: "movie"
            - name: NODE_IDX
              value: "8"
            - name: CM_ENABLED
              value: "false"
            - name: EXPIRATION_TTL
              value: "0"
            - name: SLOWPOKE_DELAY_MICROS
              value: "${SLOWPOKE_DELAY_MICROS_PLOT}"
            - name: SLOWPOKE_PROCESSING_MICROS
              value: "${SLOWPOKE_PROCESSING_MICROS_PLOT}"
            - name: SLOWPOKE_PRERUN
              value: "${SLOWPOKE_PRERUN}"
            - name: SLOWPOKE_POKER_BATCH_THRESHOLD
              value: "${SLOWPOKE_POKER_BATCH_THRESHOLD_PLOT}"
            - name: SLOWPOKE_IS_TARGET_SERVICE
              value: "${SLOWPOKE_IS_TARGET_SERVICE_PLOT}"
          ports:
            - containerPort: 3000
            - containerPort: 5550
            - containerPort: 5551
          imagePullPolicy: Always
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - worker8