kind: Service
apiVersion: v1
metadata:
  name: "composepost"
  labels:
    app: "composepost"
spec:
  selector:
    app: "composepost"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      name: http
    - protocol: TCP
      port: 5550
      targetPort: 5550
      name: zmq1
    - protocol: TCP
      port: 5551
      targetPort: 5551
      name: zmq2
  type: LoadBalancer

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "composepost"
  labels:
    app: "composepost"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "composepost"
  template:
    metadata:
      labels:
        app: "composepost"
    spec:
      containers:
        - name: "composepost"
          image: yizhengx/mucache:social
          env:
            - name: APP_PORT
              value: "3000"
            - name: APP_NAME
              value: "compose_post"
            - name: APP_NAME_NO_UNDERSCORES
              value: "composepost"
            - name: APP_NAMESPACE
              value: "social"
            - name: NODE_IDX
              value: "2"
            - name: CM_ENABLED
              value: "false"
            - name: EXPIRATION_TTL
              value: "0"
            - name: SLOWPOKE_DELAY_MICROS
              value: "${SLOWPOKE_DELAY_MICROS_COMPOSEPOST}"
            - name: SLOWPOKE_PRERUN
              value: "${SLOWPOKE_PRERUN}"
          ports:
            - containerPort: 3000
            - containerPort: 5550
            - containerPort: 5551
          imagePullPolicy: Always
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - worker1