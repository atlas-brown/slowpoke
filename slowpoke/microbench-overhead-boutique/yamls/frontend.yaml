kind: Service
apiVersion: v1
metadata:
  name: "frontend"
  labels:
    app: "frontend"
spec:
  selector:
    app: "frontend"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      name: http
    - protocol: TCP
      port: 5550
      targetPort: 5550
      name: zmq1
    - protocol: TCP
      port: 5551
      targetPort: 5551
      name: zmq2
  type: LoadBalancer

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "frontend"
  labels:
    app: "frontend"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "frontend"
  template:
    metadata:
      labels:
        app: "frontend"
    spec:
      containers:
        - name: "frontend"
          image: yizhengx/mucache:boutique-pokerpp-preregister
          env:
            - name: APP_PORT
              value: "3000"
            - name: APP_NAME
              value: "frontend"
            - name: SLOWPOKE_SERV_NAME
              value: "frontend"
            - name: APP_NAMESPACE
              value: "boutique"
            - name: NODE_IDX
              value: "2"
            - name: CM_ENABLED
              value: "false"
            - name: EXPIRATION_TTL
              value: "0"
            - name: SLOWPOKE_PROCESSING_MICROS
              value: "${SLOWPOKE_PROCESSING_MICROS_FRONTEND}"
            - name: SLOWPOKE_DELAY_MICROS
              value: "${SLOWPOKE_DELAY_MICROS_FRONTEND}"
            - name: SLOWPOKE_PRERUN
              value: "${SLOWPOKE_PRERUN}"
            - name: SLOWPOKE_POKER_BATCH_THRESHOLD
              value: "${SLOWPOKE_POKER_BATCH_THRESHOLD_FRONTEND}"
            - name: SLOWPOKE_IS_TARGET_SERVICE
              value: "${SLOWPOKE_IS_TARGET_SERVICE_FRONTEND}"
          ports:
            - containerPort: 3000
            - containerPort: 5550
            - containerPort: 5551
          imagePullPolicy: Always
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - worker5
